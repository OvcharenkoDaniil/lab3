@model List<DictionaryLibrary.DictionaryItem>
@{
    ViewBag.Title = "Home Page";
}

<div >
    <button>@Html.ActionLink("Add", "Add", "Dictionary")</button>
    <table class="table" style="margin-top: 10px">
        <thead>
        <tr>
            <th scope="col">PhoneNumber</th>
            <th scope="col">Surname</th>
            <th scope="col">RecordActions</th>
        </tr>
        </thead>
        @if (Model.Count > 0)
        {
            foreach (var record in Model)
            {
                @Html.Partial("DictionaryItem", (object)record)
            }
        }
        else
        {
            <tr>
                <td class="text-center" colspan="3">
                    <h3>There are no records in dictionary yet!</h3>
                </td>
            </tr>
        }
    </table>
</div>


@* <div> *@
@*     <button>@Html.ActionLink("Add", "Add", "Dictionary")</button> *@
@*     @foreach (var record in Model) *@
@*     { *@
@*         @Html.Partial("DictionaryItem", (object)record) *@
@*     } *@
@* </div> *@